# Initial - Notebook Work

## Installation

Use conda environment:
```sh
conda env create -f env.yml
```

Activate environment:

```sh
conda activate mlops
```

Open Jupyter notebook:

```sh
jupyter notebook
```